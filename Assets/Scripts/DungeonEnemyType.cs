/// <summary>
/// The three enemy types in Dungeon mode.
/// Each maps to one of three player input buttons (Left, Center, Right).
/// </summary>
public enum DungeonEnemyType
{
    Left   = 0,
    Center = 1,
    Right  = 2
}
